#include <stdio.h>
// int sum(int a, int b)
// {
//     return a + b;
    
// }
// for sub
int sub(int c,int d)
{
    return c-d;
}
int main()
{
    // for sum 
    // int num1, num2;
    
    // printf("The SUM : %d\n", sum(10, 10));
    // printf("The SUM : %d\n", sum(10, 0));
    // printf("The SUM : %d\n", sum(1, 10));
    // for sub
    int num3,num4;
    printf("The SUB: %d\n",sub(10,2));
    printf("The SUB: %d\n",sub(1,1));
    printf("The SUB: %d\n",sub(1,0));

    return 0;
}